DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=verify-full
PAYLOAD_SECRET=replace-with-a-long-random-secret
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Required on Vercel production; local uploads used in development without this.
BLOB_READ_WRITE_TOKEN=
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
CONTACT_EMAIL=
# Optional CLI administrator bootstrap. Do not commit real credentials.
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_NAME=Kannari Admin
